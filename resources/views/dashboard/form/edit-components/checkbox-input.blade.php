{{-- Not Completed!!! --}}

<div class="form__group">
    <div class="checkbox">
        <label for="{{ $name }}">
            <input type="checkbox" name="{{ $name }}" id="{{ $name }}" value="{{ $value }}">
            <span></span> {{ $label }}
        </label>
    </div>
</div>
